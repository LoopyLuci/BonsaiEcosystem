[package]
name = "core-ir"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Language-Agnostic Intermediate Representation for the Omnisystem"

[dependencies]
ir = { path = "../ir" }
buir = { path = "../buir" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

[dev-dependencies]

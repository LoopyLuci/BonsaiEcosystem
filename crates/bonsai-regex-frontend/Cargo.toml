[package]
name = "bonsai-regex-frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
regex = "1"
async-trait = "0.1"
core-ir = { path = "../core-ir" }
language-system = { path = "../language-system" }
